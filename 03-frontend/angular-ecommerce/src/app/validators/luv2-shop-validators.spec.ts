import { bookchorValidators } from './luv2-shop-validators';

describe('bookchorValidators', () => {
  it('should create an instance', () => {
    expect(new bookchorValidators()).toBeTruthy();
  });
});
